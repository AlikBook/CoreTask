services:
  backend:
    build: ./Backend
    ports:
      - "8080:8080"
  gateway:
    build: ./Gateway
    ports:
      - "8081:8081"
    depends_on:
      - backend
  frontend:
    build: ./Frontend
    ports:
      - "5173:80"
    depends_on:
      - gateway